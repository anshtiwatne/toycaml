{
  "name": "OCaml",
  "image": "ocaml/opam:latest",
  "postCreateCommand": "opam install -y dune ocaml-lsp-server ocamlformat && echo 'eval $(opam env)' >> ~/.bashrc",
  "customizations": {
    "vscode": {
      "extensions": [
        "ocamllabs.ocaml-platform"
      ]
    }
  }
}
